name: verified

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: verified-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  verified:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - run: echo ok
